apply from: "${rootDir}/gradle/java-project.gradle"

dependencies {
    compile(
            project(":ccc-domain"),
            libs.springContext,
            libs.springBootActuator,
            libs.logging,
            libs.projectReactor,
    )
}